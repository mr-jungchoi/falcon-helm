{{- if and .Values.node.enabled (not .Values.node.cleanupOnly) }}
{{- $imagePullSecretName := include "falcon-clusterguard.imagePullSecret" . -}}
{{- $registryConfigJson := include "falcon-clusterguard.registryConfigJson" . -}}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: falcon-sensor
  namespace: {{ include "falcon-clusterguard.namespace" . }}
  labels:
    {{- include "falcon-clusterguard.labels" . | nindent 4 }}
    app.kubernetes.io/component: clusterguard
    {{- range $key, $value := .Values.node.daemonset.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- with .Values.node.daemonset.annotations }}
  annotations:
    {{- range $key, $value := . }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "falcon-clusterguard.nodeSensorSelectorLabels" . | nindent 6 }}
  updateStrategy:
    type: {{ .Values.node.daemonset.updateStrategy }}
    {{- if and (eq .Values.node.daemonset.updateStrategy "RollingUpdate") (ne (int .Values.node.daemonset.maxUnavailable) 1) }}
    rollingUpdate:
      maxUnavailable: {{ .Values.node.daemonset.maxUnavailable }}
    {{- end }}
  template:
    metadata:
      annotations:
        {{ .Values.node.daemonset.podAnnotationKey }}: disabled
        {{- if include "falcon-clusterguard.openshiftEnabled" . | eq "true" }}
        openshift.io/scc: {{ include "falcon-clusterguard.nodeSccName" . }}
        {{- end }}
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum | quote }}
        {{- range $key, $value := .Values.node.podAnnotations }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
      labels:
        {{- include "falcon-clusterguard.labels" . | nindent 8 }}
        {{- include "falcon-clusterguard.nodeSensorSelectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: clusterguard
        {{ include "falcon-clusterguard.workloadDeployAllowlistLabel" . }}
        {{- range $key, $value := .Values.node.daemonset.labels }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
        {{- range $key, $value := .Values.node.podLabels }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
    spec:
      {{- if and (len $imagePullSecretName) (len $registryConfigJson) }}
      {{- fail "image.pullSecrets and image.registryConfigJSON cannot be used together." }}
      {{- else if or (len $imagePullSecretName) (len $registryConfigJson) }}
      imagePullSecrets:
        {{- if len $imagePullSecretName }}
        - name: {{ $imagePullSecretName }}
        {{- end }}
        {{- if len $registryConfigJson }}
        - name: {{ include "falcon-clusterguard.fullname" . }}-pull-secret
        {{- end }}
      {{- end }}
      {{- with .Values.node.daemonset.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      nodeSelector:
        kubernetes.io/os: linux
      {{- if .Values.node.daemonset.nodeAffinity }}
      affinity:
        nodeAffinity:
          {{- toYaml .Values.node.daemonset.nodeAffinity | nindent 10 }}
      {{- end }}
      securityContext:
        fsGroup: 65534
      serviceAccountName: {{ include "falcon-clusterguard.nodeServiceAccountName" . }}
      terminationGracePeriodSeconds: {{ .Values.node.terminationGracePeriod }}
      {{- if or .Values.node.daemonset.priorityClassName .Values.node.gke.autopilot }}
      priorityClassName: {{ include "falcon-clusterguard.priorityClassName" . }}
      {{- end }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      hostPID: true
      hostIPC: true
      initContainers:
      # Creates the falconstore file on the host so Kubernetes mounts it as a file
      # (not a directory), preserving the AID across container restarts.
      - name: init-falconstore
        image: {{ include "falcon-clusterguard.image" . | quote }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
        command:
          - /bin/bash
        {{ include "falcon-clusterguard.initArgs" . | nindent 8 }}
        {{- if or .Values.node.gke.autopilot .Values.node.daemonset.resources }}
        resources:
          requests:
            cpu: 10m
            ephemeral-storage: 100Mi
            memory: 50Mi
          limits:
            cpu: 10m
            ephemeral-storage: 100Mi
            memory: 50Mi
        {{- end }}
        securityContext:
          runAsUser: 0
          privileged: true
          allowPrivilegeEscalation: true
          {{- if .Values.node.gke.autopilot }}
          readOnlyRootFilesystem: true
          capabilities:
            add:
              - SYS_ADMIN
              - SYS_PTRACE
              - SYS_CHROOT
              - DAC_READ_SEARCH
          {{- else }}
          readOnlyRootFilesystem: false
          {{- end }}
        env:
          - name: POD_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: API_SERVICE_NAME
            value: {{ include "falcon-clusterguard.apiServiceName" . }}
        envFrom:
          - configMapRef:
              name: {{ include "falcon-clusterguard.configMapName" . }}
        volumeMounts:
          - name: cs-config
            mountPath: /opt/CrowdStrike/config
          - name: falconstore
            mountPath: /opt/CrowdStrike/falconstore
          - name: falcon-node-sensor-tls-certs
            mountPath: /run/secrets/tls
            readOnly: true
          - name: falcon-api-ca
            mountPath: /run/secrets/ca
            readOnly: true
      containers:
      - name: falcon-sensor
        image: {{ include "falcon-clusterguard.image" . | quote }}
        imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
        securityContext:
          runAsUser: 0
          privileged: true
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: true
          {{- if .Values.node.gke.autopilot }}
          capabilities:
            add:
              - SYS_ADMIN
              - SETGID
              - SETUID
              - SYS_PTRACE
              - SYS_CHROOT
              - DAC_OVERRIDE
              - SETPCAP
              - DAC_READ_SEARCH
              - BPF
              - PERFMON
              - SYS_RESOURCE
              - NET_RAW
              - CHOWN
              - NET_ADMIN
          {{- end }}
        {{- include "falcon-clusterguard.daemonsetResources" . | nindent 8 }}
        env:
          - name: POD_NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          - name: API_SERVICE_NAME
            value: {{ include "falcon-clusterguard.apiServiceName" . }}
        envFrom:
          - configMapRef:
              name: {{ include "falcon-clusterguard.configMapName" . }}
          {{- if include "falcon-clusterguard.falconSecretEnabled" . | eq "true" }}
          - secretRef:
              name: {{ include "falcon-clusterguard.falconSecretName" . }}
          {{- end }}
        volumeMounts:
          - name: cs-config
            mountPath: /opt/CrowdStrike/config
          - name: falconstore
            mountPath: /opt/CrowdStrike/falconstore
          {{- if .Values.cluster.enabled }}
          - name: falcon-node-sensor-tls-certs
            mountPath: /run/secrets/tls
            readOnly: true
          - name: falcon-api-ca
            mountPath: /run/secrets/ca
            readOnly: true
          {{- end }}
      volumes:
        - name: cs-config
          hostPath:
            path: /opt/CrowdStrike/config
        - name: falconstore
          hostPath:
            path: /opt/CrowdStrike/falconstore
        {{- if .Values.cluster.enabled }}
        - name: falcon-node-sensor-tls-certs
          secret:
            secretName: "falcon-node-sensor-tls"
        - name: falcon-api-ca
          secret:
            secretName: "falcon-api-ca"
        {{- end }}
{{- end }}
