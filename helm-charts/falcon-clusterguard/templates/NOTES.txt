Falcon Cluster Guard has been installed.

Release:   {{ .Release.Name }}
Namespace: {{ include "falcon-clusterguard.namespace" . }}
Version:   {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WORKLOADS
{{- if .Values.node.enabled }}
  Node Sensor DaemonSet:
    kubectl rollout status daemonset/falcon-sensor \
      -n {{ include "falcon-clusterguard.namespace" . }}
{{- else }}
  Node Sensor: disabled (node.enabled=false)
{{- end }}
{{- if .Values.cluster.enabled }}

  Cluster Sensor Deployment:
    kubectl rollout status deployment/{{ include "falcon-clusterguard.name" . }} \
      -n {{ include "falcon-clusterguard.namespace" . }}
{{- else }}

  Cluster sensor: disabled (cluster.enabled=false)
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- if .Values.cluster.enabled }}

FALCON CLUSTER SENSOR CONFIGURATION
  ValidatingWebhookConfiguration: validating.{{ .Chart.Name }}.crowdstrike.com
  Webhook Service:  webhook.{{ include "falcon-clusterguard.namespace" . }}.svc:443
  Metadata Service: api.{{ include "falcon-clusterguard.namespace" . }}.svc:443

  TLS certificates are auto-generated on install.
  {{- if .Values.cluster.autoCertificateUpdate }}
  Auto-rotation is ENABLED — certs refresh on every helm upgrade.
  {{- else }}
  Auto-rotation is DISABLED — certs are preserved across upgrades.
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- end }}
{{- if .Values.testing.enabled }}

HELM TESTS
  Run the test suite:
    helm test {{ .Release.Name }} -n {{ include "falcon-clusterguard.namespace" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{- end }}

For support, visit https://crowdstrike.com or open an issue at
https://github.com/CrowdStrike/falcon-helm
