apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "falcon-cluster-guard.nodeServiceAccountName" . }}
  namespace: {{ include "falcon-cluster-guard.namespace" . }}
  labels:
    {{- include "falcon-cluster-guard.labels" . | nindent 4 }}
    app.kubernetes.io/component: cluster-guard
  {{- if .Values.node.serviceAccount.annotations }}
  annotations: {{ toYaml .Values.node.serviceAccount.annotations | nindent 4 }}
  {{- end }}
---
{{- if .Values.admission.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "falcon-cluster-guard.admissionServiceAccountName" . }}
  namespace: {{ include "falcon-cluster-guard.namespace" . }}
  labels:
    {{- include "falcon-cluster-guard.labels" . | nindent 4 }}
    app.kubernetes.io/component: cluster-guard
  {{- if .Values.admission.serviceAccount.annotations }}
  annotations: {{ toYaml .Values.admission.serviceAccount.annotations | nindent 4 }}
  {{- end }}
{{- end }}
